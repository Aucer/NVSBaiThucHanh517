@model IEnumerable<FirstWeb.Models.Dog>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DogID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DogAge)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DogID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DogAge)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AnimalsName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AnimalsName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AnimalsName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
